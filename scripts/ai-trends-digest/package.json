{
	"name": "@side-quest/ai-trends-digest",
	"version": "1.0.0",
	"private": true,
	"description": "Weekly AI trends email digest script",
	"type": "module",
	"scripts": {
		"send": "bun run send-digest.ts",
		"send:dry-run": "bun run send-digest.ts --dry-run"
	},
	"dependencies": {
		"resend": "^4.0.0"
	},
	"devDependencies": {
		"@types/bun": "^1.3.8"
	}
}
