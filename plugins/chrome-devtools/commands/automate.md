---
description: Automate browser tasks via Chrome DevTools MCP
argument-hint: "[workflow or description]"
---

Invoke the **chrome-devtools-guide** skill to handle this browser automation request.

**Arguments received**: `$ARGUMENTS`

Pass the user's workflow description or URL to the skill. The skill handles connection checks, auth detection, snapshot-first element finding, secret safety, and graceful degradation automatically.

If no arguments are provided, ask the user what they'd like to automate in the browser.
